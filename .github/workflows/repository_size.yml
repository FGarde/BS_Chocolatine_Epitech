name: repo_size

on:
  push:
    branches: master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3.0.0

      - name: Branch_name
        run: echo -e $PWD

      - name: Exclude
        run: du -skh --exclude=".git"
